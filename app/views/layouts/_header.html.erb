<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <%# Botón que muestra/oculta la barra de navegación en pantallas chicas %>
    <button class="btn d-inline-block d-lg-none ml-auto"
      data-toggle="collapse" data-target="#navbar-usuario">
      <i class="fas fa-align-justify"></i>
    </button>

    <div class="collapse navbar-collapse" id="navbar-usuario">
      <ul class="nav navbar-nav ml-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to current_user.email, edit_user_registration_path, class: 'nav-link' %>
          </li>
        <% end %>

        <li><%= render 'devise/header/session_link' %></li>
        <li><%= render 'devise/header/registration_link' %></li>
      </ul>
    </div>
  </div>
</nav>
